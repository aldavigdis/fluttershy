<header>

	<nav class="row">
		<ul class="breadcrumb pull-right">
			<li><%= link_to "Home", "/" %> <span class="divider">/</span></li>
			<li><%= link_to "Companies", :controller => "companies", :action => "index" %> <span class="divider">/</span></li>
			<li><%= link_to @company.name, @company %> <span class="divider">/</span></li>
			<li><%= link_to "Users", :action => "index" %> <span class="divider">/</span></li>
			<li><%= link_to "New User", :action => "index" %></li>
		</ul>
	</nav>

	<div class="row">
	
		<div class="users-list span8">
		
			<h1>New user for <%= @company.name %></h1>
		
		</div>
	
	</div>

</header>

<div class="row">
	
	<div class="span8">
		
		<% if @user.errors.any? %>

		<p><%= pluralize(@user.errors.count, "error", "errors") %> came up when trying to save new user.</p>

		<% @user.errors.full_messages.each do |msg| %>
		<div class="alert">
			<%= msg %>
		</div>
		<% end %>

		<% end %>
		
		<%= render "form" %>
		
	</div>
	
	<div class="span4 pull-right">
		<%= render "shared/company_sidebar" %>
	</div>
	
</div>

<!--

<div class="row">
	
	<%= form_for @user, :url=>company_users_path(@company), :html => { :class => "span9 pull-right", :autocomplete => "off" } do |f| %>
	
	<% if @user.errors.any? %>

	<p><%= pluralize(@user.errors.count, "error", "errors") %> came up when trying to save new user.</p>

	<% @user.errors.full_messages.each do |msg| %>
	<div class="alert">
		<%= msg %>
	</div>
	<% end %>

	<% end %>
	
	<div class="control-group fullname">
		<%= f.label :fullname, "Full Name" %>
		<%= f.text_field :fullname, :class => "span4" %>
	</div>

	<div class="control-group email">
		<%= f.label :email %>
		<%= f.email_field :email, :class => "span3" %>
	</div>

	<div class="control-group password">
		<%= f.label :password %>
		<%= f.password_field :password, :class => "span3" %>
	</div>

	<div class="control-group password">
		<%= f.label :access %>
		<%= f.select :access, options_for_select(@user_access_levels_available), {}, { :class => 'span2' } %>
	</div>
	
	<div class="control-group enabled checkbox">
		<label>
			<%= f.check_box :enabled %>
			Account Enabled
		</label>
	</div>

	<div class="control-group buttons">
		<%= f.submit "Save »", :class => "btn btn-primary" %>
	</div>
	<% end %>
	
	<%= render "shared/company_sidebar" %>
		
</div>
-->
